import styles from './Camera.module.scss';

const Camera = () => {
    return (
        <div className={styles.Camera}>
            <h1>Camera</h1>
        </div>
    );
};

export default Camera;